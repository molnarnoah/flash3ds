# AVM1 (ActionScript 2) Support Status

**Status: not started.** Planned for Phase 4 (VM core) / Phase 5
(MovieClip API surface).

Target opcode set and built-in API surface are specified in
`docs/architecture.md`'s phase plan and cross-checked against real-world
usage in `docs/shift-dx-behavior.md` (see "AVM1 built-in API surface").

This file will track, per opcode/API, implementation status once Phase 4
begins — mirroring the format of `swf-support.md`.
