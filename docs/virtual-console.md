# Virtual Console resource/configuration layer

Added 2026-08-19, on top of the existing Phase 1-10 + compatibility-audit
runtime (see `CLAUDE.md` for that history). This document covers ONLY the
new resource-loading/configuration layer described below — it does not
change or re-document anything about SWF parsing, AVM1, rendering, hit-
testing, or event dispatch, all of which are reused completely unchanged.

## 1. Architecture

```
3DS application (nintendo3ds_main.cpp)
      |
      v
   RomFS (embedded in flash3ds_3ds.3dsx, see Nintendo3DSRomfs)
      |
      +-- game.swf
      |
      +-- config.ini
      |
      v
   GamePackage (src/vc/GamePackage.h)
      |
      +-- GameConfig (src/vc/GameConfig.h, backed by IniDocument)
      |
      v
   SwfLoader::loadSwf() -- UNCHANGED, existing (Phase 1)
      |
      v
   MovieClipInstance / ScriptEnvironment -- UNCHANGED, existing (Phase 5+)
      |
      +-- AVM1, MovieClip, ButtonInstance, hit testing, EventDispatcher,
      |   InputState, SceneRenderer, ExternalInterface -- ALL UNCHANGED
      |
      v
   Nintendo3DSInput -- unchanged hid-polling/edge logic; the TARGET
                        InputState key code per physical button and the
                        touch/mouse enabled flags are now configurable
                        (vc::InputMapping, part of GameConfig)
```

The new code (`src/vc/*`, `src/platform/Nintendo3DSRomfs.*`) implements
**only** resource loading, config parsing, and input-mapping configuration.
It does not reimplement or duplicate any SWF/AVM1/renderer/input logic —
see "Architectural rules" below and each file's own header comment for the
specific "why."

## 2. RomFS layout

```
romfs/
├── config.ini
└── game.swf
```

This directory is checked into the repository (`romfs/config.ini` +
`romfs/game.swf`, the latter a small clean-room demo SWF generated by
`tools/gen_3ds_demo_swf.py --swf-out romfs/game.swf` — see that script)
and embedded directly into `flash3ds_3ds.3dsx` at build time via
3dsxtool's own `--romfs=` flag (see `CMakeLists.txt`'s
`FLASH3DS_BUILD_3DS` block). **No CIA packaging, no SD-card file I/O, no
libctru `romfsInit()`** — see section 4 for why.

Both files are read at RESOLVED root, matching the layout above exactly.
There is no subdirectory support — not needed for two files, and the
Virtual Console spec's own layout doesn't call for it.

## 3. config.ini syntax

```ini
[game]
swf=game.swf

[input]
A=ENTER
B=ESCAPE
X=SPACE
Y=SHIFT
L=L
R=R
ZL=Z
ZR=V
START=ENTER
SELECT=ESCAPE
CSTICK_UP=I
CSTICK_DOWN=K
CSTICK_LEFT=J
CSTICK_RIGHT=M

[touch]
enabled=true
screen=bottom

[mouse]
enabled=true
```

This is exactly `romfs/config.ini`'s own committed content, and exactly
what `vc::GameConfig::defaults()` produces when config.ini is missing
entirely — the two are guaranteed to never drift apart, since
`defaults()` is literally `fromIniText("")` (an empty document has no
overrides to apply, so every field keeps its own hardcoded default; see
`src/vc/GameConfig.cpp`).

Grammar (`src/vc/IniDocument.h`): `[section]` headers, `key=value` pairs
(whitespace-trimmed on both sides), `;` or `#` line comments, blank lines
ignored. Deliberately tiny — no third-party INI library, matching "keep
the parser intentionally small" — the whole grammar is ~60 lines
(`IniDocument.cpp`).

### Error handling

| Condition | Behavior |
|---|---|
| config.ini missing | Use documented defaults (table above) — `GameConfig::defaults()`. |
| config.ini present but empty | Identical to "missing" — `fromIniText("")` either way. |
| Unknown `[section]` | Stored, never read by `GameConfig`, otherwise ignored. |
| Unknown key in a known section | Same — ignored. |
| Invalid value for a known key (e.g. `A=NOT_A_REAL_KEY`, `enabled=maybe`) | That ONE field falls back to its own default; every other field parses normally. Logged via `LOG_WARN("VC", ...)`. |
| Duplicate `key=` in the same section | Last one wins. |
| Malformed line (no `=`, not a comment/header) | Recorded, skipped, logged; parsing continues. |
| `game.swf` (or whatever `[game] swf=` names) missing | `GamePackage.movie->valid == false`, `errorMessage == "Resource not found: <name>"`. The 3DS entry point logs this and exits cleanly (`return 1`) — never a silent freeze/infinite loop. |
| Invalid/malformed SWF (present, but not parseable) | `SwfLoader`'s own existing, well-established error reporting (`movie->valid == false`, a specific `errorMessage`) — unchanged, reused as-is. |

## 4. Why a bespoke RomFS reader instead of libctru's `romfsInit()`

This from-source toolchain build (see `docs/3ds-toolchain.md`) excludes
`romfs_dev.c` for the exact same reason `archive_dev.c`/`console.c` are
excluded: it needs the full `sys/iosupport.h` device-table framework,
which isn't available with this project's stock (non-devkitARM-patched)
newlib. Calling `romfsInit()`/`romfsMountSelf()` would fail to **link**,
not just misbehave at runtime.

`src/platform/Nintendo3DSRomfs.h/.cpp` reimplements only the narrow
subset of `romfsMountSelf()`'s own logic that does NOT need iosupport:
locate this running `.3dsx`'s own file on the SD card (from `argv[0]`),
open it directly via `FSUSER_OpenFileDirectly` (an ordinary FS-service
call, unaffected by the exclusion), read the 3DSX header to find the
embedded RomFS section's byte offset, then read that section's own
header/directory-table/file-table (the public RomFS format, via
`<3ds/romfs.h>`'s own structs) directly via `FSFILE_Read` — entirely
without installing any devoptab/newlib virtual-filesystem device.
Deliberately scoped to a flat root directory (linear scan over the file-
sibling chain), matching this project's own two-file `romfs/` layout —
not a general-purpose RomFS implementation.

**Verified without hardware/emulator access** (same constraint Phase 10
had): the embedded RomFS section's raw bytes were manually parsed with an
independent Python script and cross-checked byte-for-byte against
`Nintendo3DSRomfs::readFile()`'s own algorithm and against the source
`romfs/config.ini`/`romfs/game.swf` files on disk — both matched exactly.
The `.3dsx`'s undefined-symbol list after linking is unchanged from Phase
10's own prior verification (only the same 8 weak libctru/C++-runtime
hooks) — confirming every new FS-service call resolved correctly. See
section 7 for the full command transcript.

**3dsxtool's own `--romfs=` quirk:** it unconditionally requires an
`--smdh=` file whenever `--romfs=` (or `--smdh=`) is used at all — reading
`3dstools/src/3dsxtool.cpp`'s `WriteExtHeader()` shows it calls
`fopen(smdhFile, "rb")` with no null check, so `--romfs=` alone would
crash. 3dsxtool does not parse or validate the SMDH's contents at all
(just its size, for offset math) — so `tools/gen_placeholder_smdh.py`
generates a minimal, correctly-**sized** (0x36C0 bytes, verified) but
purely cosmetic placeholder (solid-color icon, generic title text) at
build time. This is **not** a CIA banner/icon asset and has nothing to do
with CIAToolsR's own separate icon/banner requirements — see section 8.

## 5. Input mapping

`vc::InputMapping` (`src/vc/GameConfig.h`) holds one `int` per remappable
physical button (A/B/X/Y/L/R/ZL/ZR/START/SELECT/CSTICK_UP/CSTICK_DOWN/
CSTICK_LEFT/CSTICK_RIGHT), reusing
`runtime::InputState::KeyCode` for every named token (`ENTER`, `ESCAPE`,
`SPACE`, `SHIFT`, ... — the exact same enum `InputState`, AVM1's `Key`
object, and every other existing consumer already use) plus direct
pass-through for a single printable ASCII character (`"L"` → `'L'`,
matching the convention `Nintendo3DSInput` already used before this layer
existed). **No parallel key-code system was introduced.**

`Nintendo3DSInput` (`src/platform/Nintendo3DSInput.h/.cpp`) gained a
`const vc::InputMapping&` constructor parameter (defaulted, so every
existing call site keeps compiling). It still owns 100% of the actual
libctru `hid` polling/rescaling logic unchanged — the only thing that
became configurable is which `InputState` key code each physical button's
held-state gets written to, and whether touch/mouse feed `InputState` at
all. D-Pad → arrow keys is NOT configurable (no natural alternative
binding exists, and the spec doesn't ask for it).

**One subtlety fixed while wiring this up:** the documented default maps
both A and START to `Key.ENTER` (and B/SELECT to `Key.ESCAPE`) — the
ORIGINAL hardcoded code combined these with a single OR'd `setKeyDown`
call. A naive per-button rewrite (`setKeyDown(mapping.aKeyCode, ...)` then
`setKeyDown(mapping.startKeyCode, ...)`) would let the second call
silently stomp the first whenever two physical buttons share a target
code. `poll()` now explicitly OR-merges every physical button's flag per
DISTINCT target code before calling `setKeyDown()` (see that function's
own comment) — preserving "either button presses the logical key" exactly
like the original hardcoded version did.

**One deliberate behavior change, clearly flagged:** X/Y's documented
default changed from literal ASCII `'X'`/`'Y'` (the original hardcoded
value) to `Key.SPACE`/`Key.SHIFT`, to match this layer's own documented
default `config.ini` (AS2 content is far more likely to test
`Key.SPACE`/`Key.SHIFT` than literal `'X'`/`'Y'` character codes). L/R's
default (`'L'`/`'R'`) is unchanged.

**ZL/ZR/C-Stick (2026-08-24):** the New-3DS-only extra shoulder buttons
and second analog stick are mapped exactly like every button above — same
`applyKeyMapping()` parsing path, same `Nintendo3DSInput` OR-merge table.
ZL/ZR default to `'Z'`/`'V'` (arbitrary, documented, rebindable — see
`GameConfig.h`). The C-Stick is exposed by libctru as four independent
digital held-bits (`KEY_CSTICK_UP`/`DOWN`/`LEFT`/`RIGHT`), **not** merged
into `KEY_UP`/`DOWN`/`LEFT`/`RIGHT` the way the Circle Pad is — so it polls
like four more ordinary buttons rather than a stick with its own analog
reading, and each direction gets its own `CSTICK_UP`/`CSTICK_DOWN`/
`CSTICK_LEFT`/`CSTICK_RIGHT` config.ini key (default `'I'`/`'K'`/`'J'`/
`'M'`). These deliberately do NOT default to `Key.UP`/`DOWN`/`LEFT`/
`RIGHT` (the D-Pad's own fixed target) — the D-Pad's arrow-key
`setKeyDown()` calls happen outside the OR-merge pass, so sharing its
codes by default would let the OR-merge pass silently overwrite whatever
the D-Pad had just set that same tick.

## 6. Touch/mouse configuration

`[touch] enabled=`/`[mouse] enabled=` gate `Nintendo3DSInput::poll()`'s
existing touch→mouse translation entirely (both must be `true`; the 3DS
has exactly one pointer-like input source, so these two independently-
configurable flags gate the SAME path rather than two separate
mechanisms). `[touch] screen=` (`bottom`/`top`) selects which screen's
logical pixel dimensions `Nintendo3DSInput` is constructed with — `bottom`
(320x240) is the hardware-accurate default (the real touch digitizer is
only physically present on the bottom screen). This also fixes a
pre-existing Phase 10 quirk: the old embedded-demo app always passed the
TOP screen's dimensions regardless of which screen was actually touched
(documented in `Nintendo3DSInput.h`'s own header comment) — now genuinely
configurable instead of hardcoded.

None of `stageMouseX()`/`stageMouseY()`/viewport conversion/`InputState`
edge detection/hit testing were touched — this layer only supplies the
constructor arguments those existing mechanisms already accepted.

## 7. Development 3DSX workflow (verified this session)

```sh
cmake -S . -B build_3ds \
    -DCMAKE_TOOLCHAIN_FILE=cmake/Toolchain-3DS.cmake \
    -DFLASH3DS_3DS_TOOLCHAIN_ROOT=/path/to/3ds-toolchain
cmake --build build_3ds -j
# -> build_3ds/flash3ds_3ds.3dsx (786,520 bytes; embeds romfs/config.ini +
#    romfs/game.swf via 3dsxtool --romfs=, plus a placeholder --smdh=)
```

**Undefined symbols after link** (`arm-none-eabi-nm -u`): the same 8 weak
libctru/C++-runtime hooks Phase 10's own build already had
(`_ITM_deregisterTMCloneTable`, `_ITM_registerTMCloneTable`,
`__cxa_pure_virtual`, `__deregister_frame_info`, `__gnu_Unwind_Find_exidx`,
`__libc_fini`, `__register_frame_info`, `userAppExit`, `userAppInit`) — no
new undefined references from `Nintendo3DSRomfs`'s FS-service calls.

**RomFS section verified byte-for-byte** (no hardware/emulator available
this session, same constraint Phase 10 had): a standalone Python script
independently parsed the `.3dsx`'s embedded RomFS section (3DSX header →
`fsOffset` → RomFS header → directory/file tables → file data), replaying
the EXACT SAME algorithm `Nintendo3DSRomfs::readFile()` implements, and
confirmed both `config.ini` and `game.swf` are present at RomFS root with
content matching the source `romfs/` files exactly.

## 8. Replacing game.swf (hot-swap / recompilation independence)

**Verified this session, end to end:**

1. `romfs/game.swf` was swapped for a real, unmodified copy of "Hobo 2 —
   Prison Brawl" (~4.9 MB, SWF6/CWS).
2. `cmake --build build_3ds` was re-run — **zero C++ files were touched,
   and none were recompiled** (build log shows only the RomFS-packaging
   step re-running, not a relink of `flash3ds_3ds`).
3. The resulting `.3dsx` was re-parsed the same way as section 7, and its
   embedded `game.swf` entry matched the swapped-in Hobo 2 file
   byte-for-byte (`CWS` signature, 5,147,172 bytes).
4. `romfs/game.swf` was restored to the original clean-room demo content
   afterward, confirmed byte-identical via checksum.

**A real gap this test caught and fixed along the way:** the first
implementation packaged the `.3dsx` via a `TARGET flash3ds_3ds POST_BUILD`
custom command, which CMake only re-runs when the ELF itself relinks —
editing `romfs/game.swf` alone did NOT trigger repackaging, silently
leaving a stale `.3dsx`. Fixed by making packaging a proper
`add_custom_command(OUTPUT ...)` that explicitly `DEPENDS` on every file
under `romfs/` (via `file(GLOB_RECURSE ...)`), so a plain rebuild now
correctly picks up an EXISTING romfs file's changed *contents*. Adding a
**brand-new** file under `romfs/` (rather than editing an existing one)
still needs a re-configure to be picked up by that glob — a standard,
accepted CMake `GLOB` limitation, not something this build silently gets
wrong for the common case (swapping `game.swf`'s content).

**Runtime compilation is independent from SWF replacement**: no C++
source in this project references any specific game's content. The only
thing that changes when swapping games is `romfs/`'s contents (and,
per-title, `romfs/config.ini`'s `[game] swf=`/`[input]` values) — never
`src/`.

## 9. Real game corpus — resource-loading results (this session)

Every real-game `.swf` available in this session's uploads was run
through the desktop `flash_runtime` CLI — the exact same `SwfLoader` code
path `GamePackage`/`buildGamePackage()` calls — confirming the resource
layer is content-agnostic:

| File | Signature/Version | Frame size | Frame rate | Frame count | Parsed OK |
|---|---|---|---|---|---|
| Hobo 2 — Prison Brawl | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Hobo 3 — Wanted | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Hobo 4 — Total War | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Hobo 5 — Space Brawl | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Hobo 6 — Hell | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Hobo 7 — Heaven | CWS/6 | 600×450 px | 25 fps | 13 | ✅ |
| Extreme Pamplona | CWS/8 | 800×400 px | 24 fps | 2 | ✅ |

"Hobo 1" was not present in this session's uploads (only Hobo 2-7 were) —
`tests/games/hobo1/` already has a manifest/diagnostic from earlier
engine-compatibility work, but that predates this resource layer and
wasn't re-verified against it this session.

None of these files were committed to the repository — they're
third-party commercial game content, used here only as local validation
input (see `romfs/.gitignore`-equivalent note: `romfs/game.swf` in git is
ONLY the small clean-room demo; swapping in real content is a local,
uncommitted step, exactly as demonstrated in section 8).

## 9b. Track A A3 (2026-08-27, task #58) — Hobo1 confirmed end-to-end through this exact layer, with a worked input-mapping example

A real, unmodified copy of `hobo.swf` ("Hobo 1", ~4.97 MB, SWF6/CWS —
absent from section 9's table above, since Hobo 1 wasn't in this
project's uploads at the time that section was written) is now available
and was used to close out two remaining A3 questions: does this exact
`GamePackage`-mediated path (not `SwfLoader::loadSwf()` called directly,
which every real-corpus tool before this one used) actually load and run
it, and can `config.ini`'s existing, unmodified `[input]` grammar express
what `hobo.swf`'s own AVM1 bytecode needs.

**Input mapping — no code change needed, confirmed by evidence:**
`docs/hobo-playability-verification.md` (Finding 5/6) and
`docs/known-limitations.md` (L11) established, via static disassembly and
dynamic tracing against the real file, exactly what `hobo.swf` polls:
`Key.isDown(37/38/39/40)` (`InputState::kLeft/kUp/kRight/kDown` — see
`runtime/InputState.h`) for movement, `Key.isDown(65)`/`Key.isDown(83)`
(literal ASCII `'A'`/`'S'`, not the 3DS A button) for punch/kick-allow
gating, and `CondKeyPress=4` (`InputState::kEnd`) on nearly every frame-1
button for confirm/continue/pause navigation. The D-Pad/Circle Pad already
feed `kLeft/kUp/kRight/kDown` unconditionally (section 5 above — not
configurable, needs no `config.ini` entry). `'A'`/`'S'`/`End` are all
already-supported tokens (single-character pass-through and the existing
`"END"` named keyword — see `vc::parseKeyToken()`, `src/vc/GameConfig.cpp`)
with zero parser changes required. A worked example config.ini for a
Hobo1 package:

```ini
[game]
swf=hobo.swf

[input]
X=A
Y=S
SELECT=END
```

Parsing this exact text is now a permanent regression test —
`tests/test_vc_config.cpp`'s `GameConfig_Hobo1ExampleMapping_
ParsesToExpectedKeyCodes` — asserting `xKeyCode`/`yKeyCode`/
`selectKeyCode` resolve to `'A'`/`'S'`/`InputState::kEnd` exactly, plus the
always-on arrow codes.

**GamePackage integration — confirmed with a new tool, not committed
content:** `tools/real_game_harness/hobo_vc_package_probe.cpp` (read-only,
matching every other tool in that directory) builds a `vc::ResourceFetcher`
backed by the config text above plus a locally-supplied `hobo.swf` path
(given on the command line — never embedded in the tool's own source),
calls `vc::buildGamePackage()` (the exact same entry point
`nintendo3ds_main.cpp` calls), then runs the result through
`CharacterDictionary::build()`/`MovieClipInstance::createRoot()`/
`advanceFrame()` — the same unchanged pipeline every phase since Phase 5
has used. Run against the real file:

```
=== GameConfig parsed from the Hobo1 example config.ini ===
swfFilename           = hobo.swf
input.xKeyCode        = 65 (expect 'A'=65)
input.yKeyCode        = 83 (expect 'S'=83)
input.selectKeyCode   = 35 (expect InputState::kEnd=35)

=== movie loaded via buildGamePackage() (the exact path nintendo3ds_main.cpp uses) ===
valid=1 frameCount=13 frameRate=25.00 version=6
root created OK: currentFrame=1 displayListSize=22
after 5 ticks (with Right + 'A' held via the parsed config mapping): currentFrame=1 displayListSize=22

PASS: real hobo.swf loads and runs end-to-end through the exact GamePackage/CharacterDictionary/MovieClipInstance path nintendo3ds_main.cpp uses, with a Hobo1-shaped input mapping resolving to the expected InputState key codes.
```

**3DS cross-build — repeated section 8's own swap/rebuild/verify/restore
procedure, this time with `hobo.swf`:** `romfs/game.swf`/`romfs/config.ini`
were locally swapped for `hobo.swf` and the `[input]` example above,
`cmake --build build_3ds` was re-run (fresh configure this session — no
stale `build_3ds/` was carried over), and it built and packaged cleanly:
`flash3ds_3ds.3dsx` grew from 867,108 bytes (the clean-room demo) to
5,834,556 bytes (matching `hobo.swf`'s own ~4.97 MB), zero new undefined
symbols, only the same pre-existing "not implemented" newlib stub warnings
Phase 10 already documented as benign. `romfs/game.swf`/`romfs/config.ini`
were restored immediately after and confirmed byte-identical to their
pre-swap state via checksum (`md5sum`) — `git status --short romfs/` came
back empty, confirming nothing about this test leaked into the tracked
tree. As section 8 already established, no C++ source in this project
references any specific game's content, so this was purely a RomFS-
repackaging exercise, not a code change.

**What this does and doesn't confirm:** this closes the interpreter/
config/packaging side of A3 — `hobo.swf` genuinely loads and runs through
the exact code path the 3DS entry point uses, and a documented input
mapping resolves to precisely the key codes its own bytecode checks.
**Real hardware/emulator confirmation remains open** (A4, task #59) — this
environment has neither, same constraint Phase 10 already documented for
the clean-room demo movie.

## 10. CIA packaging boundary

**In scope / ready for CIAToolsR today:**
- A working `flash3ds_3ds.3dsx` — the ExeFS-equivalent payload — builds
  cleanly from source (section 7).
- `romfs/config.ini` + `romfs/game.swf` are exactly the RomFS payload
  CIAToolsR's own `build.bat`/`build.sh` already create an (empty)
  `romfs/` directory for.

**NOT in scope this phase / still missing (per explicit instruction — not
invented):**
- `homebrew.rsf` (the makerom recipe) — not present anywhere in this
  repository or the audited CIAToolsR upload.
- `makerom`/`bannertool` binaries — CIAToolsR's own scripts expect these
  to be supplied externally; not part of this repository.
- A REAL banner/icon for CIA packaging — CIAToolsR's `build.bat`/
  `build.sh` generate `icon.icn`/`banner.bnr` from `icon*.png`/a banner
  PNG/WAV via `bannertool`; none of those source assets exist in this
  repository. (Separately, and NOT the same thing: `dev.smdh`, generated
  by `tools/gen_placeholder_smdh.py` purely to satisfy 3dsxtool's own
  `--romfs=` prerequisite for the DEV `.3dsx` — cosmetic, cross-referenced
  in section 4, not usable as-is for a real CIA banner/icon.)
- **A real discrepancy found during this audit, worth flagging:**
  CIAToolsR's own `build.bat`/`build.sh` create an empty `romfs/`
  directory but never actually pass a `-romfs` flag to `makerom` at all —
  so even once `homebrew.rsf`/`makerom`/`bannertool`/banner/icon assets
  exist, CIAToolsR's CURRENT scripts would produce a CIA with NO RomFS
  section, silently dropping `config.ini`/`game.swf`. This is a gap in
  CIAToolsR itself (out of scope to fix here per explicit instruction —
  "do not reconstruct CIAToolsR" — flagged for whoever picks up CIA
  packaging next).

## 11. Known limitations

- No subdirectory support in the RomFS reader (flat root only — matches
  this project's own two-file layout; would need real hash-table-based
  directory traversal to extend, see `Nintendo3DSRomfs`'s own header
  comment).
- ~~`Nintendo3DSRomfs::open()` only supports a plain SD-card `.3dsx` launch~~
  — **superseded, see the 2026-08-24 addendum below.** `3dslink:/` and a
  non-homebrew-ABI fallback (`ARCHIVE_ROMFS`) are both implemented in the
  actual `Nintendo3DSRomfs.cpp` this section was originally describing;
  this bullet was never updated after that follow-up work landed. An
  NCCH/CIA launch path is still not implemented.
- Not independently verified on real hardware or an emulator this session
  (no device/emulator access) — verification was done by exhaustively
  cross-checking the produced `.3dsx`'s binary layout against the exact
  algorithm the ARM code implements (section 7) and against
  `docs/3ds-toolchain.md`'s own prior Phase-10 boot confirmation (Azahar)
  for everything this layer builds on top of unchanged.
- `[input]` remapping covers A/B/X/Y/L/R/ZL/ZR/START/SELECT/C-Stick
  (four directions) — the D-Pad/Circle-Pad's arrow-key mapping is fixed
  (see section 5). *(Updated 2026-08-24: ZL/ZR/C-Stick were added this
  session — previously only A/B/X/Y/L/R/START/SELECT were configurable.)*
- Config values are read once at startup; there is no live-reload of
  `config.ini` while the app is running.

## 12. Addendum (2026-08-24): ported forward into a divergent tree

This document, and every file it describes (`src/vc/*`,
`src/platform/Nintendo3DSRomfs.*`, `romfs/`, `tools/gen_placeholder_smdh.py`,
the `test_vc_*.cpp` suites), was originally written in a session working
against a local GitHub Desktop checkout (`G:\3DS\GITHUB\flash3ds`) that had
diverged from this repository's own history at this layer's original
2026-08-19 fork point. That local checkout's own `CLAUDE.md` had NOT yet
picked up this repository's later compatibility-audit-phase roadmap work
(MP3 decode, `IFileLoader`/`LocalFileLoader`, the `memory_breakdown`/
`mem_profile_check` diagnostic tooling, and the M2 lazy-`CharacterDictionary`
memory optimization) — the two trees grew independently from the same
ancestor.

This layer was ported into this repository on 2026-08-24 by reading every
file out of that local checkout and re-integrating it against this
repository's CURRENT state, rather than overwriting anything: `CharacterDictionary`
here keeps its lazy-parsing (Option B) form; `Nintendo3DSInput` gained the
`vc::InputMapping` constructor parameter on top of (not instead of) its
existing behavior; `nintendo3ds_main.cpp` keeps every `MemoryDiagnostics`
checkpoint while switching from the embedded-demo movie to
`GamePackage`/`Nintendo3DSRomfs`; `Log` gained the `setDebugCallback` second
output path this layer's own diagnostics rely on. Desktop `ctest` (348/348
passing, up from the pre-merge 316) and a full 3DS cross-build (zero new
undefined symbols beyond the existing 8 weak libctru/C++-runtime hooks, a
successfully-packaged `flash3ds_3ds.3dsx` with its RomFS section
generated by this same build) both verified the merge — see `CLAUDE.md`'s
"Virtual Console layer" section for the fuller provenance note and next
steps (in particular: `docs/current-state-audit*.md` and
`docs/implementation-roadmap*.md` were NOT updated to reflect this layer
existing — read this document, not those, for VC-specific status).

## 13. Environment reset (2026-08-29), `argv[0]`-null crash fix, and a log-free `kInvalidMovie` diagnostic

**Provenance note first:** partway through the session that produced this
entry, this sandbox's git history was silently reset backward again (the
8th such reset this project has hit — see `git log`'s
`8dce581 "Restore full tree after 7th environment reset..."` commit,
itself evidence of the 7th). Real work done earlier in that same session —
a real linear-gradient fill-rendering implementation, and a first pass at
the `argv[0]` fix below — was committed at the time but is **no longer
in this repository's git history or working tree**; only this section's
own work (built fresh, from scratch, after noticing the reset) is real
and verified here. If a later session finds gradient rendering still
missing (`ShapeTessellator`'s `PaintKind`/`GradientPaint`,
`IRenderer::fillPolygonGradient`, etc. — grep for `fillPolygonGradient`
to check), that work needs to be redone, not assumed lost-but-recoverable
from this repo. Always `git log`/`grep` to verify a prior session's
specific claimed commit is actually present before relying on it — do not
trust a summary of earlier turns in the same session over the repo's own
current state, precisely because this has now happened multiple times.

**Bug 1 — `argv[0]`-null crash on Azahar's "Load File" launch path,**
found from a real user crash report (a solid-red screen, no on-screen
square markers at all — i.e. not even this app's own graceful error
path) plus the exact Azahar Log Viewer output they captured, showing
`HW.Memory <Error> ... unmapped Read32 @ 0x00000000 at PC 0x001239A0`.
Resolved via `arm-none-eabi-addr2line`/`objdump` against the unstripped
`build_3ds/flash3ds_3ds` ELF to `nintendo3ds_main.cpp`'s
`romfs.open(argv[0], &romfsFailure)` call, faulting instruction
`ldr r1, [r5]` — confirming `argv` itself (the array pointer, not just
`argv[0]`'s string contents) was NULL. `Nintendo3DSRomfs::open()` already
handles a null `argv0` *value* gracefully (`OpenFailure::kNullArgv0`),
but that check only runs once `open()` is entered — it can't protect
against `argv` itself being null, which faults evaluating `argv[0]` at
the call site, before `open()` is ever called. Apparently Azahar's direct
"Load File" `.3dsx` launch path doesn't populate `argv` the way a
homebrew-launcher (HBL/Rosalina forwarder) does. **Fix:** guard with both
`argc` and `argv` before indexing:

```cpp
const char* argv0 = (argc > 0 && argv != nullptr) ? argv[0] : nullptr;
if (!romfs.open(argv0, &romfsFailure)) {
```

`Nintendo3DSRomfs::open()`'s existing `kNullArgv0` handling then takes
over exactly as designed.

**Bug 1 confirmed genuinely fixed on real hardware/Azahar:** after this
fix, the user's next test showed a DIFFERENT result (2 white squares on
the top screen, solid red, no crash at all — a real screen capture,
frame-extracted and pixel-inspected to count the squares precisely) —
this is `FatalError::kInvalidMovie`, this app's own graceful "the
configured SWF failed to load" diagnostic, reached only once
`Nintendo3DSRomfs::open()` has already returned true. So the crash really
is gone; what's left is a second, different, real problem.

**Finding 2 — `kInvalidMovie` on a real Hobo1 RomFS package, cause not
yet resolved.** The exact `.3dsx` the user tested had its embedded RomFS
`game.swf` independently extracted (a small standalone Python 3DSX/RomFS
parser, parsing the `ThreeDsxHeader` + `<3ds/romfs.h>` `romfs_header`/
`romfs_dir`/`romfs_file` layouts directly from the built file — not
assumed) and confirmed byte-for-byte identical to the real `hobo.swf`
(md5 `d59ed276a15e17fe6e08254ad0e51738`) — and that exact extracted copy
parses cleanly through this project's own `SwfLoader` on desktop
(`flash_runtime --quiet`: `Status: OK`, SWF6, 13 frames, 3745 tags, zero
errors). So the embedded file is NOT corrupt and NOT unparseable in
general — the failure is specific to the 3DS runtime path, most likely
something about a single ~4.97 MB `FSFILE_Read` (`Nintendo3DSRomfs::
readAt`) that has never actually been exercised on real hardware/emulator
before (only much smaller files, and this sandbox's own non-3DS render
harness, had loaded `hobo.swf`-sized content until now).

**Also discovered while investigating:** this app's own `[VC]`/`[3DS]`
`LOG_*` output (routed through `svcOutputDebugString`, see `Log.h`'s
`setDebugCallback`) did **not** appear anywhere in the user's Azahar Log
Viewer capture, even for a run that demonstrably executed
`Nintendo3DSRomfs::open()` successfully (reaching `kInvalidMovie` at all
requires that call to have already returned `true`, and `open()`'s very
first line unconditionally logs at entry). Azahar's default Log Viewer
filter apparently excludes this app's debug-level output, making log
capture an unreliable diagnostic channel for this class of question going
forward — **don't rely on asking for another log capture as the next
step for a similar failure; prefer the on-screen square-count mechanism
below, or a build-time diagnostic tool, when logs have already proven
silent for this app once.**

**Fix (partial — a diagnostic, not yet a root-cause fix): a `kInvalidMovie`
sub-code, reusing `showFatalErrorScreen`'s existing bottom-screen
square-count mechanism** (see that function's own doc comment — "no font
rendering... or log access at all" was already its explicit design goal;
this reuses it rather than reinventing a font-free diagnostic). `vc::
GamePackage` gained a new `bool swfResourceFound` field (`src/vc/
GamePackage.h`), set from `buildGamePackage()`'s own `fetch()` return
value (`src/vc/GamePackage.cpp`) — `true` means the configured SWF's bytes
WERE fetched and `SwfLoader` itself rejected them (a parse-level
failure); `false` means `fetch()` itself failed (for `Nintendo3DSRomfs::
readFile` specifically, this covers both "no such RomFS entry" and "entry
found but its `readAt()` call failed", e.g. exactly the large-single-read
scenario above). `nintendo3ds_main.cpp`'s `kInvalidMovie` call site now
passes `subCode = package.swfResourceFound ? 2 : 1`, drawn on the BOTTOM
screen. Two existing `tests/test_vc_game_package.cpp` cases
(`GamePackage_MissingSwf_ProducesInvalidMovieWithClearError` /
`GamePackage_MalformedSwf_ProducesInvalidMovie`) gained a
`CHECK(package.swfResourceFound)` assertion each, pinning the field to
exactly the case it's meant to distinguish. 382/382 desktop tests
passing, zero new warnings.

**Still open — the actual root cause of Bug 2.** Once the next test
build's bottom-screen square count comes back (1 or 2), that narrows
which of the two `haveSwf` branches in `buildGamePackage()` is failing;
if it's `subCode=1` (fetch failure), the next step is almost certainly
adding chunked reads to `Nintendo3DSRomfs::readAt()`/`readFile()` (looping
`FSFILE_Read` in smaller pieces, e.g. 64 KiB, and accumulating) rather
than assuming a single ~5 MB `FSFILE_Read` call always succeeds on real
hardware/every emulator — this project's own custom RomFS reader has
never had that assumption exercised at this size before. Do not implement
that chunking fix speculatively before the subCode confirms which branch
is actually failing — this project's evidence-first methodology applies
here same as everywhere else.

**Resolved (2026-08-29, without ever needing the subCode above): the very
next real-device test, on the v11 diagnostic build, loaded successfully**
— no `kInvalidMovie` screen at all. So Bug 2's actual failure mode must
have been transient/build-specific rather than a deterministic large-read
limitation (the subCode instrumentation stays in place regardless — it's
cheap and still useful if this class of failure recurs). **This is the
first real hardware/emulator-confirmed successful load of the real
`hobo.swf` (Hobo1) this project has ever had** — the top screen rendered
recognizable real content (the character portrait, "HOBO" wordmark, and
button row matching the Armor Games-hosted title/menu art), confirming
`SceneRenderer`/`ShapeTessellator`/`CharacterDictionary` are all working
correctly against the real ~5 MB file on-device, not just in this
sandbox's own non-3DS render harness.

Two follow-on observations from that same test, neither a regression from
this session's fixes:

1. **Input mapping was wrong in v10/v11.** Both were built by swapping
   `romfs/game.swf` for real `hobo.swf` but leaving `romfs/config.ini` at
   its clean-room-demo defaults (`X=SPACE`/`Y=SHIFT`/`SELECT=ESCAPE`),
   not the Hobo1-specific mapping `tests/test_vc_config.cpp`'s
   `GameConfig_Hobo1ExampleMapping_ParsesToExpectedKeyCodes` already
   pins (`X=A`/`Y=S`/`SELECT=END` — confirmed by static AVM1 disassembly,
   see `docs/hobo-title-progression.md`, to be the keys this specific
   file's frame-1 buttons actually poll). v12 fixes this — see that
   test case, or `romfs/config.ini`'s own comments during a Hobo1 swap,
   for the exact mapping to use on any future swap. Per
   `docs/hobo-title-progression.md`'s own already-established finding,
   correcting this does NOT make a "loading screen" get dismissed —
   there is no root-timeline gate to dismiss in this file at all (a
   single-frame/`onEnterFrame`-driven game); `SELECT`/`END` instead
   drives the one real dispatched action (a pause/quit-to-portal menu).
   Don't re-investigate "why doesn't any key advance past the title
   screen" as if it were a bug — it isn't, by design, in this specific
   SWF.

2. **A real, unaddressed FPS finding, likely the SAME `25/30fps` lock
   already on this project's queue** (see `CLAUDE.md`'s deferred-work
   list) — now visible for the first time on the real Hobo1 content,
   where it was previously invisible only because Hobo1 could never
   successfully load on real hardware before v11. Azahar's own overlay
   reported `App: 10 FPS`, `Képkocka: 4.36 ms` — the huge gap between a
   4.36 ms measured render time and a 10 FPS actual rate rules out the
   GPU blit itself as the bottleneck and points at CPU-side work outside
   what that counter measures. Traced (not yet fixed) to
   `nintendo3ds_main.cpp`'s main loop (~line 458): the frame-rate-pacing
   comment there already explains that `root->advanceFrame()` is
   correctly throttled to the SWF's own declared rate (`vblanksPerSwfFrame`,
   2 for Hobo1's 25 fps against a 60 Hz vblank) — but `scene.render(...)`
   is called EVERY vblank regardless, unconditionally re-tessellating
   every on-screen shape from scratch each time
   (`SceneRenderer.cpp`'s `renderShapeCharacter`/`renderMorphShapeCharacter`
   both call `tessellateShape()` fresh, with no cache at all, every
   single call). For Hobo1's real content (2,990 shapes + 19 morph
   shapes) that's real, non-trivial CPU work happening roughly twice as
   often as necessary at minimum (once per vblank instead of once per
   actual movie-frame change), and with zero caching between calls even
   when the movie frame hasn't changed at all. This is a strong,
   evidence-backed hypothesis, not yet a confirmed-and-fixed root cause —
   no profiling has actually isolated tessellation cost specifically yet,
   and per this project's own rules, a fix shouldn't be written
   speculatively before that measurement happens.
